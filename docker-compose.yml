services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.prod

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      - backend
