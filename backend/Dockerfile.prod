FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir uv
ENV UV_LINK_MODE=copy

COPY pyproject.toml uv.lock ./
RUN uv sync --no-install-project

COPY . .
RUN uv sync

EXPOSE 8000

CMD ["uv", "run", "uvicorn", "ai_calculator.main:app", "--host", "0.0.0.0", "--port", "8000"]
